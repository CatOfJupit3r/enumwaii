---
"enumwaii": patch
---

Refresh the npm package README with the new enumwaii branding and current documentation links, and allow compatible `@standard-schema/spec` 1.x releases to be deduplicated in consumer projects.
