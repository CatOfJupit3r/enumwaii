---
"eslint-plugin-enumwaii": minor
---

Let `enforce-enum-casing` consume the same rationale-required declaration exceptions as `no-object-em` and `no-manual-enum`, so consumers can share one reviewed exception list across rules while retaining existing wildcard options.
