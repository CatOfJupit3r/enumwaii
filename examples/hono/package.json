{
  "name": "@enumwaii/example-hono-drizzle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsdown",
    "build:cloudflare": "wrangler deploy --dry-run --outdir dist-cloudflare",
    "build:library": "pnpm --dir ../../packages/enumwaii build",
    "start": "node dist/server.mjs",
    "test": "vitest run",
    "test:types": "tsc --noEmit",
    "dev:bun": "pnpm run build:library && bun --watch runtime/bun.ts",
    "dev:deno": "pnpm run build:library && deno task --config deno.json dev",
    "dev:cloudflare": "pnpm run build:library && pnpm run types:cloudflare && wrangler dev",
    "types:cloudflare": "wrangler types --config wrangler.jsonc",
    "test:bun": "pnpm run build:library && tsc --noEmit -p tsconfig.bun.json && bun test ./runtime-tests/bun.test.ts",
    "test:deno": "pnpm run build:library && deno task --config deno.json test",
    "test:cloudflare": "pnpm run build:library && pnpm run types:cloudflare && tsc --noEmit -p tsconfig.cloudflare.json && vitest run --config vitest.cloudflare.config.ts && pnpm run build:cloudflare",
    "test:runtimes": "pnpm run test:bun && pnpm run test:deno && pnpm run test:cloudflare",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@hono/node-server": "2.1.1",
    "@hono/standard-validator": "0.4.0",
    "drizzle-orm": "0.45.2",
    "enumwaii": "file:../../packages/enumwaii",
    "hono": "4.13.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.2",
    "@types/bun": "1.4.0",
    "@types/node": "26.4.0",
    "drizzle-kit": "0.31.10",
    "tsdown": "0.22.14",
    "tsx": "4.23.13",
    "typescript": "6.0.3",
    "vitest": "4.1.11",
    "wrangler": "4.127.1"
  }
}
