/**
 * Optional string transformations for callback-based enumwaii derivation.
 *
 * Import these helpers from `enumwaii/derive-with` only when a derived value
 * follows one of the supplied transformations.
 *
 * @module derive-with
 */

export { lowercase } from "./lowercase";
export { uppercase } from "./uppercase";
